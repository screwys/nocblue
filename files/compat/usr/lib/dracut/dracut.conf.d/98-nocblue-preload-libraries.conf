# systemd exports libno_rlimit_as through LD_PRELOAD before switch-root.
# Keep the library and the final loader cache in every generated initramfs.
install_items+=" /usr/lib64/libno_rlimit_as.so /etc/ld.so.cache "
