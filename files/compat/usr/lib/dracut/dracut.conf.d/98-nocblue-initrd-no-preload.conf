# Keep Secureblue's global allocator preload out of early userspace.
# The module masks only the initramfs copy; the real-root configuration remains active.
add_dracutmodules+=" nocblue-initrd-no-preload "
