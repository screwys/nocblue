# Temporary mitigation for Dirty Frag until Fedora ships fixed kernels.
# Blocks the vulnerable xfrm ESP and RxRPC modules from autoload or manual load.
install esp4 /bin/false
install esp6 /bin/false
install rxrpc /bin/false
